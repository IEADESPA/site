# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.astro
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local
api/local.settings.json

# Internal release/workflow notes
AGENTS.md
RELEASE_CHECKLIST.md

# Ghost theme kept locally as a design reference, not part of this theme
Journal-main/

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
test-results
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# SOPS + age — nunca versionar chaves privadas (ficam fora do repositório,
# em %APPDATA%\sops\age\keys.txt no Windows ou ~/.config/sops/age/keys.txt).
# O texto puro dos segredos (.env.local) já é ignorado pela regra *.local acima.
keys.txt
*.agekey
age.key
